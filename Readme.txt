This project demonstrates a video decoder.
The input video is splited into many small pieces
and are transcoded concurently.
After they are all converted, these files are mergered back
into output file.
Using ffmpeg library, so need to install it before the programm
can run properly